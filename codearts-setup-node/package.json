{
  "name": "set-upnode",
  "version": "0.0.1",
  "description": "CodeArts Actions 插件 - 安装指定版本的 Node.js 环境",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "package-main": "ncc build src/main.ts -o dist && shx mv dist/index.js dist/main.js",
    "package-stop": "ncc build src/stop.ts -o dist && shx mv dist/index.js dist/stop.js",
    "compile": "npm run build && npm run package-main && npm run package-stop",
    "zip": "rimraf dist/src && rimraf  dist/tests  && node scripts/package-zip.js",
    "package": "npm run compile && npm run zip",
    "test": "jest"
  },
  "private": true,
  "directories": {
    "test": "tests"
  },
  "author": "侯建彬",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "axios": "^1.9.0",
    "chalk": "^5.4.1",
    "inquirer": "^12.6.0",
    "package-lock.json": "^1.0.0",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^18.18.6",
    "@vercel/ncc": "^0.36.1",
    "archiver": "^5.3.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.2",
    "typescript": "^5.4.5",
    "rimraf": "^6.0.1",
    "shx": "^0.4.0"
  }
}
